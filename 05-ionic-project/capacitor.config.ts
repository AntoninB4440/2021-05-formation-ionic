import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '05-ionic-project',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
